#include <assert.h>
#include <stdio.h>
#include <stdlib.h>
#include <stdint.h>
#include <string.h>

#define CAP 12345

struct Heap {
    float *arr;
    size_t len;
    size_t cap;
};

struct Heap heapify(float *arr) {
}

int main() {
    malloc(CAP * sizeof(float))
}
